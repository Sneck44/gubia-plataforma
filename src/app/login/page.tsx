import {login} from "./actions";export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="min-h-screen bg-[var(--cream)] grid place-items-center p-6"><div className="card w-full max-w-md p-8"><div className="text-2xl font-bold tracking-[.18em] text-[var(--gubia)]">GUBIA</div><p className="text-xs tracking-[.2em] text-slate-500">ACCESO DE PERSONAL</p><h1 className="text-2xl font-semibold mt-8">Iniciar sesión</h1><p className="text-slate-500 mt-2">Acceso exclusivo para personal autorizado.</p>{q.error&&<div className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">{q.error}</div>}<form action={login} className="grid gap-4 mt-6"><input name="email" type="email" required autoComplete="email" placeholder="Correo electrónico" className="rounded-xl border p-3"/><input name="password" type="password" required autoComplete="current-password" placeholder="Contraseña" className="rounded-xl border p-3"/><button className="rounded-xl bg-[var(--gubia)] text-white p-4 font-semibold">Entrar al panel</button></form><a href="/" className="inline-block mt-6 text-sm">← Volver a GUBIA</a></div></main>}